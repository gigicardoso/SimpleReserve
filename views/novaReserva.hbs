<div class="formulario-reserva-sala">
  <h1 class="tituloSala">Cadastre uma Reserva</h1>
  <form id="formReserva" method="POST" action="/novareserva">
    <div class="formulario-ask">
      <label for="eventName">*Nome do evento:</label>
      <input
        type="text"
        id="eventName"
        name="nome_evento"
        placeholder="Digite o nome do evento"
      />
    </div>
    <div class="formulario-ask">
      <label for="campusSala">*Sala do campus:</label>
      <select id="campusSala" name="cod_salas" required>
        <option value="">Selecione uma sala</option>
        {{#each salas}}
          <option value="{{cod}}">Sala {{numero}}</option>
        {{/each}}
      </select>
    </div>
    <div class="formulario-ask">
      <label for="eventDate">*Data do evento:</label>
      <input type="date" id="eventDate" name="date" />
    </div>

    <div class="formulario-ask">
      <label for="eventTimeStart">*Hora início:</label>
      <input type="time" id="eventTimeStart" name="hora_inicio" />
    </div>

    <div class="formulario-ask">
      <label for="eventTimeEnd">*Hora final:</label>
      <input type="time" id="eventTimeEnd" name="hora_final" />
    </div>

    <div class="formulario-ask">
      <label for="observations">Observações:</label>
      <textarea
        id="observations"
        name="obs"
        placeholder="Digite observações adicionais"
      ></textarea>
    </div>

    <input type="hidden" name="cod_usuarios" value="1" />

    <div class="divider"></div>

    <!-- Aqui pode entrar o aviso de sucesso/erro futuramente -->

    <div class="button-group">
      <button class="clear-btn" type="reset">Limpar</button>
      <button class="save-btn" type="submit">Salvar Evento</button>
    </div>
  </form>
</div>
{{#each salas}}
  <p>{{cod}} Sala {{numero}}</p>
{{/each}}